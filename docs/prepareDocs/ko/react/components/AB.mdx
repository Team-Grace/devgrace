import { AB } from '@devgrace/react';

# AB

`AB 테스트`할 때 사용할 수 있는 컴포넌트입니다. 

아래와 같이 올바르게 사용하지 않으면 `null`을 반환합니다.

- 올바르지 않은 컴포넌트 타입을 사용 (사용 가능한 컴포넌트: AB.CaseA. AB.CaseB)
- 중복된 컴포넌트 타입을 사용
- 자식 컴포넌트 길이가 `2`가 아닌 경우

<br />

## Interface
```tsx
interface ABProps {
  onAction?: () => void;
}

const AB: {
  ({ children }: PropsWithChildren): JSX.Element | null;
  CaseA({ children, onAction }: PropsWithChildren<ABProps>): JSX.Element;
  CaseB({ children, onAction }: PropsWithChildren<ABProps>): JSX.Element;
};
```

## Usage

```tsx
import { AB } from '@devgrace/react';

const { CaseA, CaseB } = AB;

const Example = () => {
  return (
    <AB>
      <CaseA onAction={() => console.log("Case A Renader")}>
        <div>A</div>
      </CaseA>
      <CaseB onAction={() => console.log("Case B Renader")}>
        <div>B</div>
      </CaseB>
    </AB>
  );
}; 
```

## Example

<div>
  <p>페이지를 새로고침 하면 컴포넌트가 변경됩니다.</p>
  <AB>
    <AB.CaseA onAction={() => console.log("Case A Renader")}>
      <div style={{ width: "50px", backgroundColor: "green", color: "white", fontSize: '2rem', textAlign: 'center'}}>A</div>
    </AB.CaseA>
    <AB.CaseB onAction={() => console.log("Case B Renader")}>
      <div style={{width: "50px", backgroundColor: "green", color: "white", fontSize: '2rem', textAlign: 'center'}}>B</div>
    </AB.CaseB>
  </AB>
</div>